# 얼마씩 (eolmassik) — 기획·설계 문서

> 가입 없이 링크 하나로 끝내는 1/N 정산

> 버전 0.1 · 2026-09-15 · 작성자 유은정

---

## 1. 개요

### 1.1 한 줄 정의

**얼마씩**은 여러 명이 함께 쓴 돈을 **항목별로 입력하고, 누가 얼마씩 더 낼지 조정한 뒤, 카카오톡으로 공유**하는 모바일 웹 서비스.

| | |
|---|---|
| 서비스명 | 얼마씩 |
| 영문 slug | `eolmassik` |
| 레포 | `eolmassik` (백엔드 폴백 추가 시 `eolmassik-api`) |
| 도메인 | Vercel 기본 도메인(`*.vercel.app`). 커스텀 도메인은 사지 않음 |
| 태그라인 | 가입 없이 링크 하나로 끝내는 1/N 정산 |

이름의 근거 — 정산 상황에서 실제로 나오는 말이 "우리 얼마씩 내면 돼?" 이고, 서비스가 내놓는 답도 "민수는 14,000원씩" 이다. 질문과 답이 같은 단어를 쓰므로 별도 설명이 필요 없다.

### 1.2 문제 정의

기존 정산 방식의 불편함:

| 방식 | 문제 |
|---|---|
| 카톡에서 암산 | 항목이 3개만 넘어가도 계산이 틀림. 검증 불가 |
| 엑셀/메모장 | 만들기 귀찮고, 공유하면 상대가 못 알아봄 |
| 기존 정산 앱 | 가입·친구추가·앱 설치가 필요. 일회성 정산에는 과함 |

특히 **"이건 우리가 낼게"** 같은 현실적인 조정이 어느 도구에서도 깔끔하게 처리되지 않음. 결국 누군가 손으로 다시 계산하게 됨.

### 1.3 핵심 가치

1. **설치·가입 없음** — 링크 열면 바로 씀
2. **불균등 부담을 정식 기능으로 지원** — "우리가 8천원 더 낼게"가 예외가 아니라 일급 기능
3. **공유가 결과물** — 계산이 끝이 아니라, 카톡에 붙여넣을 수 있는 형태가 최종 산출물

### 1.4 비목표 (v1에서 하지 않는 것)

- 영수증 촬영·OCR
- 회원가입, 로그인, 친구 관계
- 실제 송금 실행, 정산 완료 추적
- 실시간 동시 편집
- 다중 통화

---

## 2. 사용자 시나리오

### 시나리오 A — 캠핑 정산 (주 시나리오)

> 5명이 캠핑을 갔다. 고기값 32,000원과 마트 장보기 30,000원은 은정이 카드로 긁었고, 바베큐장 이용료 25,000원도 은정이 냈다. 은정이네는 부부가 함께 왔고 고기를 많이 먹어서 고기값은 조금 더 내기로 했다. 바베큐장 비용은 은정이네가 전부 내겠다고 했다.

1. 정산 이름 "9/15 캠핑" 입력
2. 참여자 5명 이름 입력 (은정이네, 민수, 철준, 영희, 지훈)
3. 항목 3개 입력
4. 고기 항목에서 "은정이네가 12,000원 더 부담" 설정
5. 바베큐장 항목에서 "은정이네 전액 부담" 설정
6. 결과 확인 → 카톡 공유

### 시나리오 B — 점심 정산

> 팀 6명이 점심을 먹었다. 한 명은 안 먹고 커피만 마셨다.

1. 참여자 프리셋 "점심팀" 불러오기
2. 항목 입력, 커피만 마신 사람은 밥 항목에서 체크 해제
3. 공유

### 시나리오 C — 공유받는 쪽

> 민수가 카톡으로 링크를 받았다.

1. 링크 클릭 → 읽기 전용 결과 화면
2. 내가 얼마 보내야 하는지, 왜 그 금액인지(항목별 내역) 확인
3. 필요하면 송금 앱으로 이동

---

## 3. 데이터 모델

### 3.1 전체 구조

```
Settlement (정산)
├─ id: string
├─ title: string              // "9/15 캠핑"
├─ createdAt: number
├─ participants: Participant[]
├─ items: Item[]
└─ options: Options
```

### 3.2 Participant (참여자)

```ts
interface Participant {
  id: string;
  name: string;        // "은정이네", "민수"
  headcount: number;   // 기본 1. "은정이네"처럼 여러 명을 한 참여자로
                       // 넣었을 때 N빵 가중치로 사용
}
```

> **설계 결정 — 그룹 개념 대신 headcount**
> 커플·가족을 별도의 "그룹" 엔티티로 만들지 않고, 참여자 이름을 "은정이네"로 넣고 `headcount: 2`를 주는 방식을 택함. 데이터 구조가 평평하게 유지되고 UI가 단순해짐. 대부분의 경우 가족은 어차피 한 지갑이므로 개인으로 쪼갤 실익이 없음.
> `headcount`가 필요한 유일한 이유는 **N빵 시 2인분 부담**이 자연스러워야 하기 때문. 기본값 1이라 신경 쓰지 않아도 동작하고, 필요할 때만 조정하는 선택적 필드로 둠.

### 3.3 Item (항목)

```ts
interface Item {
  id: string;
  name: string;              // "고기", "바베큐장 이용료"
  amount: number;            // 32000
  payerId: string;           // 실제로 결제한 사람
  participantIds: string[];  // 이 항목을 부담하는 사람들
  extraCharges: ExtraCharge[]; // 추가 부담 (아래 3.4)
}
```

> **설계 결정 — 결제자와 부담자 분리**
> `payerId`(돈을 실제로 낸 사람)와 `participantIds`(돈을 부담해야 할 사람)를 반드시 분리해야 함. 이게 있어야 최종적으로 "누가 누구에게 얼마 보내면 되는지"가 계산됨. 둘을 합치면 정산이 아니라 단순 나누기 계산기가 됨.

### 3.4 ExtraCharge (추가 부담) — 핵심 기능

사용자가 요청한 두 가지 케이스가 **같은 개념의 변주**이므로 하나로 통합함:

```ts
interface ExtraCharge {
  participantId: string;
  type: 'amount' | 'full';
  value?: number;   // type이 'amount'일 때만
}
```

| 요구사항 | 표현 | 계산 |
|---|---|---|
| "13,000원 중 우리가 8,000원 낼게" | `{ type: 'amount', value: 8000 }` | 8,000원을 먼저 떼고, 남은 5,000원을 나머지 인원이 N빵 |
| "바베큐 비용은 우리가 낼게" | `{ type: 'full' }` | 전액을 해당 인원이 부담. 나머지는 0원 |

`full`은 `amount`가 항목 전액인 특수 케이스이므로 계산 로직은 동일. UI에서만 "전액" 토글로 따로 노출함.

### 3.5 Options

```ts
interface Options {
  rounding: 'none' | 'ceil10' | 'ceil100';  // 원 단위 처리
  roundingAbsorber: 'payer' | 'split';      // 잔돈을 누가 흡수할지
}
```

---

## 4. 계산 로직

### 4.1 항목별 부담액 계산

```
function calculateItem(item, participants):

  1. 추가 부담 먼저 확정
     for each extraCharge in item.extraCharges:
       charged[pid] += (type === 'full' ? item.amount : value)

  2. 남은 금액
     remaining = item.amount - sum(charged)

  3. 남은 금액을 N빵
     - 분모 = item.participantIds 중 'full' 부담자를 제외한 인원들의
              headcount 합계
     - 각자 = remaining × (자기 headcount / 분모)

  4. 반올림 처리 후 잔차를 흡수자에게 몰아줌
```

**검증용 예제 (시나리오 A의 고기 항목)**

```
금액: 32,000원 / 참여자 5명
  은정이네(headcount 2), 민수(1), 철준(1), 영희(1), 지훈(1)
추가 부담: 은정이네 12,000원

1. charged[은정이네] = 12,000
2. remaining = 32,000 - 12,000 = 20,000
3. 분모 = 2 + 1 + 1 + 1 + 1 = 6
   은정이네 = 20,000 × 2/6 = 6,667
   민수/철준/영희/지훈 = 20,000 × 1/6 = 3,333 (각)
4. 합계 = 6,667 + 3,333×4 = 19,999 → 잔차 1원을 결제자가 흡수

최종 부담액
  은정이네 = 12,000 + 6,667 + 1 = 18,668
  나머지 4명 = 3,333씩
  검산: 18,668 + 13,332 = 32,000 ✓
```

### 4.2 최종 송금 계산 (Debt Simplification)

1. 참여자별 **순액 = (내가 결제한 총액) − (내가 부담해야 할 총액)**
2. 순액이 양수면 채권자(받을 사람), 음수면 채무자(보낼 사람)
3. 채권자·채무자를 금액 큰 순으로 정렬해 그리디하게 매칭

이렇게 하면 송금 건수가 최대 `(참여자 수 − 1)`건으로 줄어듦. 6명이면 최대 15건이 날 수 있는 것을 5건 이하로 압축.

```
예: 은정(+37,000), 민수(-14,000), 철준(-14,000), 영희(-9,000)
→ 민수→은정 14,000 / 철준→은정 14,000 / 영희→은정 9,000  (3건)
```

### 4.3 반올림 정책

나눠떨어지지 않는 금액이 대부분이므로 정책이 반드시 필요함.

- 기본값: `none` + 잔차는 **결제자가 흡수** (결제자가 총대를 멘 사람이므로 1~4원 손해를 감수하는 게 사회적으로 자연스러움)
- 옵션: 100원 단위 올림 — 실제 송금 시 금액이 깔끔해지지만, 합계가 총액을 초과하므로 초과분은 결제자에게 이득으로 돌아감. 이 점을 UI에 명시해야 함

---

## 5. 화면 설계

### 5.1 화면 구성

```
[1] 홈 / 정산 시작
      ↓
[2] 참여자 입력
      ↓
[3] 항목 입력  ←→  [3-1] 항목 상세 (참여자 선택 / 추가 부담)
      ↓
[4] 결과 & 공유
      ↓
[5] 공유받은 결과 (읽기 전용, 링크 진입)
```

### 5.2 [2] 참여자 입력

- 이름 입력 후 엔터 → 칩 형태로 추가되는 방식. 모바일에서 키보드를 내리지 않고 연속 입력 가능해야 함
- 각 칩을 탭하면 `headcount` 조정 (1 → 2 → 3, 기본 1이라 대부분 건드릴 일 없음)
- **프리셋 저장/불러오기** — 최근 사용한 참여자 조합을 localStorage에 저장. "캠핑 멤버", "점심팀"

### 5.3 [3] 항목 입력

- 한 줄 = 한 항목. `[항목명] [금액]` 두 필드만 노출한 컴팩트한 행
- 신규 항목 추가 시 **기본값은 전원 참여**. 빼는 게 더하는 것보다 빈도가 높음
- 각 행에 요약 뱃지로 상태 표시 (예: `5명 N빵`, `은정이네 +12,000`, `은정이네 전액`)
- 행을 탭하면 상세로 펼침

### 5.4 [3-1] 항목 상세

두 가지만 있으면 됨:

1. **참여자 토글** — 전원 선택된 상태에서 빠지는 사람만 해제
2. **추가 부담 설정** — "누가 더 낼래?" 버튼 → 사람 선택 → 금액 입력 또는 "전액" 토글

> 추가 부담 UI에서 금액 입력 시 **실시간으로 "나머지 N명이 각 X원"** 을 보여줘야 함. 8,000원을 넣었을 때 남은 사람들이 얼마 내는지 즉시 보이지 않으면, 사용자가 얼마를 넣어야 할지 판단할 수 없음.

### 5.5 [4] 결과 & 공유

- 상단: 총액, 인원, 항목 수
- 중단: **송금 리스트** (가장 중요한 정보. 최상단 배치)
- 하단: 항목별 내역 (접힌 상태, 펼쳐서 검증 가능)
- 액션: `텍스트 복사` / `카카오톡 공유` / `링크 복사`

### 5.6 [5] 공유받은 결과

- 링크로 진입 시 읽기 전용
- **"나는 누구?" 선택** — 이름을 고르면 본인 금액을 강조 표시. 받는 사람은 자기 금액만 궁금함
- `이 정산 복제해서 수정하기` 버튼으로 편집 모드 전환 가능

---

## 6. 공유 설계

### 6.1 텍스트 복사 (1순위)

실사용 빈도가 가장 높음. 카톡 대화창에 그대로 붙여넣는 형태.

```
🧾 9/15 캠핑  ·  총 87,000원

· 고기 32,000  (5명 N빵, 은정이네 12,000 추가부담)
· 바베큐장 25,000  ← 은정이네 전액
· 마트 30,000  (5명 N빵)

💸 정산
민수 → 은정이네   14,000원
철준 → 은정이네   14,000원
영희 → 은정이네    9,000원
지훈 → 은정이네    9,000원

자세히 보기 ▸ https://...
```

- 이모지는 최소한만. 카톡에서 줄맞춤이 깨지지 않도록 공백 정렬은 피하고 단순 나열 사용

### 6.2 URL 링크 (2순위)

**백엔드 없이 URL에 상태를 인코딩하는 방식**을 채택.

```
정산 객체 → JSON 최소화(키 축약) → LZ-string 압축 → base64url
→ https://도메인/s#<encoded>
```

- `#` 뒤에 두면 서버 로그에 데이터가 남지 않고, 서버 측 URL 길이 제한도 적용받지 않음

**길이 실측 (lz-string, 축약 키 기준)**

| 참여자 | 항목 | JSON 원본 | 압축 후 URL 전체 |
|---|---|---|---|
| 3명 | 3개 | 228자 | 298자 |
| 5명 | 10개 | 535자 | 586자 |
| 6명 | 20개 | 943자 | 884자 |
| 8명 | 30개 | 1,534자 | 1,200자 |
| 10명 | 50개 | 2,690자 | 1,687자 |
| 12명 | 100개 | 5,851자 | 2,831자 |

항목 구조가 반복되기 때문에 항목이 늘어도 **선형 이하로만** 길어짐. 20항목 지점부터는 압축 결과가 원본 JSON보다 짧아짐. 실사용 범위(참여자 10명 / 항목 30개 이내)에서 URL 길이는 문제가 되지 않음.

**키 축약의 효과** — 6명/20항목 기준 풀 키 1,544자 → 축약 키 863자. 객체를 배열로 눕히고 키를 한 글자로 줄이는 것만으로 44% 절감. 축약 스키마는 인코딩 레이어에만 두고 앱 내부 모델은 풀 키를 유지할 것.

**실제 제약과 폴백 전략**

브라우저 URL 한계보다 아래 세 가지가 먼저 걸림:

1. 카카오 공유 SDK의 링크 URL 길이 제한 (2,000자 부근에서 잘린 사례 있음 — 구현 전 카카오 문서로 확인 필요)
2. QR 코드 — 300~500자를 넘으면 인식률이 떨어짐 (QR 공유를 넣을 경우)
3. 심리적 저항 — 카톡에 수천 자짜리 URL이 뜨면 수신자가 클릭을 꺼림

따라서 **하이브리드 방식**을 채택:

```
URL 길이 < 1,500자  →  인코딩된 URL 그대로 공유  (실사용 대부분)
URL 길이 ≥ 1,500자  →  단축 백엔드로 폴백
```

폴백은 `{짧은키: 압축데이터}`를 30일 TTL로 저장하는 수준이면 충분함. 배포를 Vercel로 정했으니 Vercel Functions에 KV 계열 스토어를 붙이면 되고, 무료 티어로 감당됨. 평소에는 백엔드를 전혀 타지 않으므로 장애 지점과 비용이 거의 발생하지 않음. 이 저장소가 생기면 P3의 완료 체크 기능을 붙일 기반이 이미 마련된다는 부수 효과도 있음.

> 폴백 백엔드의 구체적인 스토어 선택은 **미확정**이다. P1에서 실제로 1,500자를 넘는 사례가 나오는 시점에 정한다.

> **장점:** 서버·DB·인증 없이 정적 호스팅으로 배포 완료
> **한계:** 공유 후 원본을 수정하면 이전 링크는 옛 데이터를 보여줌. 수정 시 링크를 다시 공유해야 한다는 안내 필요

### 6.3 카카오톡 공유

- Kakao JavaScript SDK `Share.sendDefault()` — 피드 템플릿
- 제목: 정산 이름 / 설명: 총액·인원 / 버튼: "정산 내역 보기"
- 카카오 디벨로퍼스 앱 등록 + 사이트 도메인 등록 필요. Vercel 배포 도메인을 그대로 등록한다
- SDK 로드 실패나 PC 환경 대비 **링크 복사 폴백**을 반드시 둘 것

---

## 7. 기능 우선순위

### P0 — 이게 없으면 서비스가 아님

- [ ] 참여자 입력
- [ ] 항목 입력 (이름, 금액, 결제자)
- [ ] 항목별 참여자 선택
- [ ] 균등 N빵 계산
- [ ] 추가 부담 (금액 지정 / 전액)
- [ ] 최소 송금 계산
- [ ] 결과 화면
- [ ] 텍스트 복사

### P1 — v1 출시에 포함

- [x] URL 링크 공유 + 읽기 전용 결과 화면
- [ ] 1,500자 초과 시 단축 URL 백엔드 폴백
- [x] 카카오톡 공유 SDK 연동
- [ ] 참여자 headcount
- [ ] 참여자 프리셋 저장 (localStorage)
- [ ] 반올림 옵션
- [ ] 작성 중인 정산 임시 저장 (새로고침 복구)
- [ ] "나는 누구?" 본인 금액 강조

### P2 — 다음 버전

- [ ] **공통비 비례 분배** — 배달비·봉사료·주차비를 각자 부담액 비율대로 자동 배분
- [ ] **할인/쿠폰** — 위와 동일 로직에 음수 금액으로 처리
- [ ] **송금 딥링크** — 토스/카카오페이 송금 링크에 금액까지 채워서 제공
- [ ] 정산 히스토리 (로컬)
- [ ] 항목별 가중치 분배 (2인분, 0.5인분)

### P3 — 백엔드가 필요해지는 시점

- [ ] 정산 완료 체크 (받는 사람이 "보냈음" 표시)
- [ ] 수정 시 기존 링크에 반영
- [ ] 여러 명이 동시에 항목 추가

---

## 8. 기술 스택

> 아래는 확정된 스택이다. 실제 버전은 `package.json` 이 단일 출처다.

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | React 19 + Vite 8 | 정적 배포로 충분. SSR 불필요 |
| 언어 | TypeScript 7 (strict) | 계산 로직에 타입이 안전망 역할. 네이티브 포트라 타입 검사가 5.9 대비 약 3배 빠름 |
| 라우팅 | TanStack Router (파일 기반) | 라우트가 `/` 와 `/s` 둘뿐이지만 `autoCodeSplitting` 으로 진입 번들을 줄임 |
| 상태관리 | Zustand 5 | 단일 Settlement 객체만 다루므로 가벼운 것으로 충분. `persist` 로 임시 저장까지 해결 |
| 스타일 | Tailwind CSS 4 | 모바일 우선 레이아웃 빠르게. v4 는 CSS-first 설정이라 config 파일이 없음 |
| URL 인코딩 | lz-string | 압축률 대비 용량이 작음 |
| 입력 검증 | Zod 4 | 공유 URL 은 남이 편집할 수 있으므로 디코드 결과를 반드시 검증 |
| 공유 | Kakao JS SDK | |
| 배포 | Vercel | 정적 호스팅. Git 연동으로 main 머지 시 자동 배포 |
| 테스트 | Vitest 5 + fast-check | **계산 로직은 반드시 단위 테스트** (아래 참조) |
| 린트·포맷 | Biome 2 | ESLint + Prettier 2종 대신 1종으로 통합 |
| CI | GitHub Actions | PR 마다 lint / build / audit 검사 |

### 8.1 테스트가 필요한 이유

돈 계산이 1원이라도 틀리면 서비스 신뢰가 무너짐. 최소한 다음은 테스트로 고정할 것:

- 항목 부담액 합계 == 항목 금액 (모든 케이스에서)
- 전체 부담액 합계 == 전체 결제액 합계
- 추가 부담이 항목 금액을 초과할 때의 처리
- `full` 부담자가 여러 명일 때의 분배
- 참여자가 1명일 때, 금액이 0원일 때 등 엣지 케이스

합계 불변식은 예제 테스트가 아니라 **fast-check property 테스트**로 고정한다. 랜덤 입력 수백 건으로
검증하면 손으로 나열하지 못한 조합(추가 부담 중복, 전액 부담자 복수, headcount 편중)을 알아서 찾아낸다.

### 8.2 개발 워크플로

- 브랜치 전략 — 기능 브랜치 → `develop` → `main`. `main` 과 `develop` 은 직접 push 와 삭제가 금지되어 있고 PR 로만 변경된다
- PR 이 열리거나 갱신되면 GitHub Actions 가 lint / build / audit 을 검사한다. 모두 통과해야 머지할 수 있다
- `main` 에 머지되면 Vercel 이 프로덕션으로 배포한다
- AI 에이전트 작업 규칙은 `CLAUDE.md` 에 있다

---

## 9. 주요 의사결정 기록

| # | 결정 | 근거 | 재검토 시점 |
|---|---|---|---|
| 1 | 영수증 OCR 미포함 | 입력 항목이 보통 3~5개라 수동 입력이 더 빠름. OCR 정확도 보정 비용이 큼 | 장보기 정산 수요가 확인되면 |
| 2 | 그룹 대신 headcount | 데이터 구조 평탄화. "은정이네"를 한 참여자로 다루는 게 실제 사용에 부합 | 개인별 내역 요구가 나오면 |
| 3 | 추가 부담을 단일 개념으로 통합 | `full`은 `amount`의 특수 케이스. 계산 로직 하나로 유지 | — |
| 4 | 백엔드 없이 URL 인코딩 (+ 1,500자 초과 시 단축 폴백) | 가입·설치 없음이라는 핵심 가치를 지키는 가장 싼 방법. 실측 결과 실사용 범위에서 URL 길이는 문제되지 않음 | 완료 체크 기능이 필요해지면 |
| 5 | 로그인 없음 | 일회성 정산에서 가입 요구는 이탈의 가장 큰 원인 | 히스토리 동기화 수요가 생기면 |
| 6 | 잔차는 결제자 흡수 | 총대 멘 사람이 몇 원 감수하는 것이 사회적으로 자연스러움 | — |
| 7 | 기본 결제자를 두고 예외 항목만 변경 | 항목마다 결제자를 고르게 하면 입력이 늘고, 대부분의 정산은 한 명이 몰아서 결제함. `Settlement.defaultPayerId` 로 모델에 반영 | 여러 명이 나눠 결제하는 사례가 빈번해지면 |
| 8 | 배포를 Vercel로 결정 | 정적 호스팅이면 충분하고 Git 연동만으로 배포가 끝남. 폴백 백엔드가 생겨도 같은 플랫폼에서 처리 가능 | — |
| 9 | 린트·포맷을 Biome 단일 도구로 | ESLint + Prettier 2종을 유지할 만큼 규칙이 복잡하지 않음. 설정 파일 하나, 실행 속도 이점 | 커스텀 규칙이 필요해지면 |
| 10 | 합계 불변식을 property 테스트로 고정 | 예제 테스트만으로는 손으로 나열하지 못한 조합을 놓침. 돈 계산은 그 조합에서 틀림 | — |
| 12 | 항목 요약은 결제자만 항상 적고 나머지는 예외만 적음 | 5.3 의 예시(`5명 N빵`)대로 모든 행에 기본값을 적으면 열 줄 중 아홉 줄이 같은 말이라 예외인 줄이 묻힘. 결제자는 목록에서 확인할 다른 길이 없어 예외로 둠 | 항목당 표시할 정보가 더 늘어나면 |
| 13 | 결제자 없는 항목을 기본 결제자로 일괄 지정 | 기본 결제자를 참여자에서 지우면 그 사람이 결제한 항목이 한꺼번에 정산에서 빠지는데, 항목마다 상세 시트를 열게 하는 비용이 큼. 이미 결제자가 있는 항목은 덮지 않음 | — |
| 14 | 기본 결제자는 참여자 칩·상세 시트에서 지정 | 사람에 붙는 속성이라 참여자 쪽에 둠. 항목 카드에 칩 줄을 하나 더 두면 바로 위 참여자 칩과 같은 이름이 두 번 늘어섬. 해제는 두지 않음 — 비우면 새 항목이 결제자 없이 만들어짐 | 항목별 결제자 지정이 기본 사용 흐름이 되면 |
| 15 | 시트의 삭제는 같은 시트를 확인 화면으로 바꿔서 묻고, 하단에 "적용" 을 둠 | 되돌리기가 없어서 삭제가 즉시 일어나면 잘못 눌렀다는 걸 사라진 뒤에 알게 됨. 확인을 버튼 자리에 인라인으로 펼치면 시트 높이가 변해 화면이 밀려 올라가므로, 시트 내용을 통째로 갈아 끼우고 좌상단 뒤로 가기로 돌아옴. 시트를 겹쳐 띄우지 않는 이유는 배경이 두 번 어두워지고 포커스 가두기가 두 겹이 되기 때문 | — |
| 11 | TypeScript 7 채택 | 실측 결과 타입 검사가 5.9 대비 약 3배 빠르고(0.84s → 0.28s), 이 스택에서 걸린 건 `baseUrl` 제거와 `vite/client` 타입 추가뿐이었음. 앱 코드만 있고 라이브러리를 배포하지 않아 문제가 생겨도 영향 범위가 좁음 | 7.x 에서 막히는 문제가 나오면 `~5.9` 로 되돌림 |
